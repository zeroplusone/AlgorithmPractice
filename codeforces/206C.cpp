#include<iostream>
#include<cstdio>
#include<cstdlib>
#include<cstring>
using namespace std;

#define MAX 100100

int w[MAX];

int main()
{
	int n,l,r,ql,qr;
	int dpl,dpr;]
	while(scanf("%d%d%d%d%d",&n,&l,&r,&ql,&qr));
	{
		for(int i=0;i<n;++i)
			scanf("%d",&w[i]);
		dpl=
		dpr=
		if(dpl<dpr)
			printf("%d\n",dpl);
		else
			printf("%d\n",dpr);
	}
	return 0;
}