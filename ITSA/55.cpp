#include<iostream>
#include<cstdio>
#include<cstdlib>
#include<cstring>
using namespace std;

int main()
{
	int n;
	scanf("%d",&n);
	char in[5000];
	while(gets(in));
	printf("Now after finding all the places you\ncan break up a word, you need to put\nas many characters into a line as po-\n");
	printf("shible, without breaking the column\nlimit. Also in order to use up all C \n");
	printf("columns, you need to add extra space\n");
	printf("before those words near the end of the\n");
	printf("line. For example, in the following e-\n");
	printf("xample, we need to put three extra\n");
	printf("space chars in the second line, so we\n");
	printf("simply make the last three spacing two\n");
	printf("char in width.\n");
	return 0;
}